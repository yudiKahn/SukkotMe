import React, { Fragment } from 'react';

const Spinner = () => {
    return (<Fragment>
        <i className="fas fa-spinner text-center" style={{ margin:'auto', display:'block',fontSize:150}}></i>
    </Fragment>)
}

export default Spinner;
